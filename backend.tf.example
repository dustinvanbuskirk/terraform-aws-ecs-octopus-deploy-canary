# backend.tf.example
# Copy this file to backend.tf and configure for your remote state backend
# backend.tf can be committed to version control (without sensitive values)

# Option 1: S3 Backend (Recommended for AWS)
# Uncomment and configure:
#
# terraform {
#   backend "s3" {
#     bucket         = "my-terraform-state-bucket"
#     key            = "ecs-canary/terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#     dynamodb_table = "terraform-state-lock"
#     
#     # Optional: Use specific profile
#     # profile = "my-aws-profile"
#   }
# }

# Option 2: Terraform Cloud
# Uncomment and configure:
#
# terraform {
#   cloud {
#     organization = "my-organization"
#     
#     workspaces {
#       name = "ecs-canary-deployment"
#     }
#   }
# }

# Option 3: Azure Storage Backend
# Uncomment and configure:
#
# terraform {
#   backend "azurerm" {
#     resource_group_name  = "my-resource-group"
#     storage_account_name = "mystorageaccount"
#     container_name       = "terraform-state"
#     key                  = "ecs-canary.tfstate"
#   }
# }

# Option 4: Google Cloud Storage Backend
# Uncomment and configure:
#
# terraform {
#   backend "gcs" {
#     bucket = "my-terraform-state-bucket"
#     prefix = "ecs-canary"
#   }
# }

# Setup Instructions for S3 Backend:
# 
# 1. Create an S3 bucket for state storage:
#    aws s3 mb s3://my-terraform-state-bucket --region us-east-1
#
# 2. Enable versioning on the bucket:
#    aws s3api put-bucket-versioning \
#      --bucket my-terraform-state-bucket \
#      --versioning-configuration Status=Enabled
#
# 3. Create a DynamoDB table for state locking:
#    aws dynamodb create-table \
#      --table-name terraform-state-lock \
#      --attribute-definitions AttributeName=LockID,AttributeType=S \
#      --key-schema AttributeName=LockID,KeyType=HASH \
#      --provisioned-throughput ReadCapacityUnits=5,WriteCapacityUnits=5
#
# 4. Enable server-side encryption:
#    aws s3api put-bucket-encryption \
#      --bucket my-terraform-state-bucket \
#      --server-side-encryption-configuration '{
#        "Rules": [{
#          "ApplyServerSideEncryptionByDefault": {
#            "SSEAlgorithm": "AES256"
#          }
#        }]
#      }'
#
# 5. Block public access:
#    aws s3api put-public-access-block \
#      --bucket my-terraform-state-bucket \
#      --public-access-block-configuration \
#        BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true